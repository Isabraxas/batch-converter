spring:
  application:
    name: batchconverter
  profiles:
    active: ${PROFILE_ACTIVE:default}
  cloud:
    config:
      #uri: http://localhost:7015
      uri: ${CLOUD_URI:http://desarrollo.viridian.cc:7000}
      profile: default